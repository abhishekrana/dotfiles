# TODO

- [ ] tmux-resurrect/continuum + agent sidebar: verify after a real
      reboot that restore relaunches sidebars (`@resurrect-processes`)
      and the plugin adopts them — no ghost 30-col panes, no squeezed
      layouts. Auto-restore rebuilt stale saves on top of live sessions
      once (2026-07-07); halt file `~/tmux_no_auto_restore` skips it.
