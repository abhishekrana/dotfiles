# LazyVim rewrites these; formatting them just creates churn.
nvim/.config/nvim/lazyvim.json
nvim/.config/nvim/lazy-lock.json
# Vendored upstream themes.
bat/.config/bat/themes/
yazi/.config/yazi/flavors/
# The invented GitLab fixture and the golden files the tests diff against. Both are
# compared byte for byte, so formatting them would fail the tests that prove the
# renders still match.
apps/agentbar/internal/workdesk/fixture/
apps/agentbar/internal/workdesk/testdata/
